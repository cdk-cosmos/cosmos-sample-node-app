{
  "name": "sample",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "docker build -t ${ECR_URL}:${APP_BUILD_VERSION} . && docker push ${ECR_URL}:${APP_BUILD_VERSION}"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
